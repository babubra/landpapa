# КалининградЗем — Сайт продажи земельных участков

Сайт для продажи земельных участков в Калининградской области.

## Структура проекта

```
Test1/
├── backend/                 # FastAPI бэкенд (порт 8001)
│   ├── app/
│   │   ├── models/          # SQLAlchemy модели
│   │   ├── routers/         # API эндпоинты
│   │   ├── schemas/         # Pydantic схемы
│   │   ├── main.py          # Точка входа FastAPI
│   │   ├── database.py      # Подключение к БД
│   │   ├── config.py        # Настройки (.env)
│   │   ├── auth.py          # JWT + Argon2
│   │   └── seed.py          # Тестовые данные
│   ├── requirements.txt
│   └── venv/
├── kaliningrad-land/        # Next.js фронтенд (порт 3000)
│   ├── src/
│   │   ├── app/             # Страницы (App Router)
│   │   ├── components/      # React компоненты
│   │   └── lib/             # Утилиты
│   └── package.json
├── admin/                   # Next.js админ-панель (порт 3001)
│   ├── src/
│   │   ├── app/             # Страницы
│   │   ├── components/      # UI компоненты
│   │   └── lib/auth.tsx     # AuthContext
│   └── package.json
└── docker-compose.yml       # PostgreSQL + PostGIS
```

---

## 🚀 Быстрый запуск (после перезагрузки)

### ⚡ Одной командой (Windows)
```powershell
.\dev.ps1
```
Скрипт запустит Docker, backend, frontend и admin в отдельных окнах.

---

### Или по отдельности:

#### 1. База данных
```bash
docker-compose up -d
```

#### 2. Backend (терминал 1)
```bash
cd backend

# macOS / Linux:
./venv/bin/uvicorn app.main:app --reload --port 8001

# Windows:
# .\venv\Scripts\uvicorn app.main:app --reload --port 8001
```
**API:** http://localhost:8001

#### 3. Публичный сайт (терминал 2)
```bash
cd kaliningrad-land
npm run dev
```
**Сайт:** http://localhost:3000

#### 4. Админ-панель (терминал 3)
```bash
cd admin
npm run dev -- --port 3001
```
**Админка:** http://localhost:3001

---

## 🔐 Авторизация (Админка)

**Тестовый аккаунт:**
- Логин: `admin`
- Пароль: `Admin123`

### API эндпоинты авторизации

| Метод | URL | Описание |
|-------|-----|----------|
| POST | `/api/auth/login` | Логин → JWT токен |
| GET | `/api/auth/me` | Текущий пользователь |

---

---

## 🐳 Docker и развёртывание

Проект полностью контейнеризирован и готов к работе в Docker. Для локальной разработки используется `docker-compose.yml`, для продакшена — `docker-compose.prod.yml`.

### Имена контейнеров (Production)
При запуске через `docker-compose.prod.yml` используются следующие имена:
- `landpapa_db` — База данных (PostgreSQL + PostGIS)
- `landpapa_backend` — API сервер (FastAPI)
- `landpapa_frontend` — Публичный сайт (Next.js)
- `landpapa_admin` — Панель управления (Next.js)
- `landpapa_nginx` — Обратный прокси и SSL

---

## 🚀 Обновление на продакшене

### Быстрый деплой (одной командой)
```bash
cd ~/landpapa
./deploy.sh
```

Скрипт автоматически:
- Получит обновления из Git
- Пересоберёт контейнеры с `--build`
- Применит миграции БД
- Обновит настройки (seed)
- Очистит старые Docker образы

### Или вручную:

1. **Зайти в папку проекта:**
   ```bash
   cd ~/landpapa
   ```

2. **Затянуть свежий код из GitHub:**
   ```bash
   git pull origin main
   ```

3. **Пересобрать и запустить контейнеры:**
   ```bash
   docker compose -f docker-compose.prod.yml up -d --build
   ```

4. **Применить миграции базы данных:**
   ```bash
   docker exec landpapa_backend alembic upgrade head
   ```

5. **Обновить базовые настройки (seed):**
   ```bash
   docker exec landpapa_backend python -m app.seed
   ```

---

---

## ⚠️ Полный сброс базы данных (Hard Reset)

> [!CAUTION]
> **Это уничтожит ВСЕ данные в базе (объявления, новости, настройки).** Используйте только если база сломана или требуется полная очистка. Файлы картинок (`uploads`) останутся.

### Инструкция для восстановления с нуля:

1. **Остановить контейнеры и удалить volume базы:**
   ```bash
   docker compose -f docker-compose.prod.yml down
   docker volume rm landpapa_postgres_data
   ```

2. **Запустить чистое окружение:**
   ```bash
   docker compose -f docker-compose.prod.yml up -d --build
   ```

3. **Создать таблицы и настройки:**
   ```bash
   docker exec landpapa_backend python -m app.seed
   ```
   *Скрипт `seed.py` теперь автоматически регистрирует все модели и создает таблицы.*

4. **Синхронизировать миграции:**
   ```bash
   docker exec landpapa_backend alembic stamp head
   ```
   *Это пометит базу как "актуальную", чтобы будущие миграции не пытались пересоздать существующие таблицы.*

---

## 🤖 Для ИИ-помощников: Правила деплоя

> [!IMPORTANT]
> При работе на продакшене следуйте этим правилам БЕЗ ИСКЛЮЧЕНИЙ:

1. **Контекст**: Всегда работайте в папке `~/landpapa`.
2. **Пароли**: Никогда не меняйте пароли в `docker-compose.yml`, если они заданы в `.env`. Если возникает ошибка авторизации, проверьте `.env`.
3. **Миграции (Стандарт)**:
   - После `git pull` всегда запускайте: `docker exec -it landpapa_backend alembic upgrade head`
   - Если возникает ошибка `DuplicateTable` или `DuplicateColumn` — значит база рассинхронизирована.
     - **Решение**: Проверьте, соответствует ли код состоянию базы. Если да, используйте `alembic stamp head`.
4. **PostGIS**: 
   - Никогда не создавайте миграции, которые делают `DROP TABLE` для таблиц `spatial_ref_sys`, `geometry_columns` или других таблиц PostGIS.
   - Если `alembic revision --autogenerate` сгенерировал удаление этих таблиц — **УДАЛИТЕ ЭТИ СТРОКИ** из файла миграции.
5. **Фронтенд**: 
   - Любые изменения в `kaliningrad-land` требуют пересборки: `docker compose -f docker-compose.prod.yml up -d --build frontend`
6. **Seed**:
   - `python -m app.seed` — безопасен для повторного запуска (он проверяет наличие данных перед созданием). Запускайте его после обновления, чтобы применить новые настройки по умолчанию.

---

## 📦 Первоначальная установка (с нуля)

### 1. База данных
```bash
docker-compose up -d db          # только db, без бэкенда
```

### 2. Backend

**macOS / Linux:**
```bash
cd backend
python3 -m venv venv             # ⚠️ только при ПЕРВОМ запуске

# Установка зависимостей:
./venv/bin/pip install -r requirements.txt

# Инициализация данных:
./venv/bin/python -m app.seed    # ⚠️ только при ПЕРВОМ запуске

# Запуск:
./venv/bin/uvicorn app.main:app --reload --port 8001
```

**Windows:**
```powershell
cd backend
python -m venv venv              # ⚠️ только при ПЕРВОМ запуске

# Установка зависимостей:
.\venv\Scripts\pip install -r requirements.txt

# Инициализация данных:
.\venv\Scripts\python -m app.seed    # ⚠️ только при ПЕРВОМ запуске

# Запуск:
.\venv\Scripts\uvicorn app.main:app --reload --port 8001
```

### 3. Публичный сайт
```bash
cd kaliningrad-land
npm install                      # ⚠️ только при ПЕРВОМ запуске
npm run dev
```

### 4. Админ-панель
```bash
cd admin
npm install                      # ⚠️ только при ПЕРВОМ запуске
npm run dev
```

---

## База данных (PostgreSQL + PostGIS)

### Модели

| Модель | Описание |
|--------|----------|
| `News` | Новости (slug, title, content, is_published) |
| `Reference` | Справочники (type: land_use, land_category) |
| `Realtor` | Риэлторы (name, phone, email, company) |
| `Owner` | Владельцы участков (приватно) |
| `District` | Районы (name, slug) |
| `Settlement` | Населённые пункты (district_id, name, slug) |
| `Listing` | Объявления (title, description, realtor_id, settlement_id) |
| `Plot` | Участки (listing_id, cadastral_number, area, price, geometry) |
| `Lead` | Заявки на обратный звонок (name, phone, status, source) |
| `AdminUser` | Администраторы (username, password_hash) |

### Единицы измерения

- **Площадь**: хранится в **м²**, отображается в **сотках** (÷100)
- **Цена**: рубли

---

## Backend API

### Публичные эндпоинты

| Метод | URL | Описание |
|-------|-----|----------|
| GET | `/api/news` | Список новостей |
| GET | `/api/news/{slug}` | Новость по slug |
| GET | `/api/listings` | Каталог объявлений (с фильтрами) |
| GET | `/api/listings/{slug}` | Детальное объявление |
| GET | `/api/locations/districts` | Районы с количеством объявлений |
| GET | `/api/locations/settlements` | Населённые пункты |
| GET | `/api/references?type=` | Справочники |
| POST | `/api/leads/public` | Отправка заявки на обратный звонок (с Honeypot) |

### Фильтры `/api/listings`

| Параметр | Тип | Описание |
|----------|-----|----------|
| `page` | int | Номер страницы |
| `size` | int | Размер страницы (макс 100) |
| `district_id` | int | Фильтр по району |
| `settlement_id` | int | Фильтр по нас. пункту (устаревший) |
| `settlements` | str | Список ID нас. пунктов через запятую (например: "7,8,9") |
| `land_use_id` | int | Фильтр по назначению |
| `price_min` / `price_max` | int | Диапазон цены |
| `area_min` / `area_max` | float | Диапазон площади (м²) |
| `sort` | str | newest / price_asc / price_desc |

---

## 🌍 Переменные окружения (для продакшена)

При деплое на продакшен нужно настроить переменные окружения для указания URL бэкенда.

### Публичный сайт (`kaliningrad-land/`)

Создайте файл `.env.local` или `.env.production`:
```env
NEXT_PUBLIC_API_URL=https://api.your-domain.com
```

### Админ-панель (`admin/`)

Создайте файл `.env.local` или `.env.production`:
```env
NEXT_PUBLIC_API_URL=https://api.your-domain.com
NEXT_PUBLIC_SITE_URL=https://your-domain.com
```

- `NEXT_PUBLIC_API_URL`: URL вашего бэкенда.
- `NEXT_PUBLIC_SITE_URL`: URL вашего фронтенда (важно для SEO, sitemap и канонических ссылок).

> **Примечание:** Для локальной разработки переменные не требуются — по умолчанию используются `http://localhost:8001` и `http://localhost:3000`.

### ⚡ Проксирование API (для разработчиков)

В режиме локальной разработки Next.js проксирует API-запросы на backend через **rewrites** в `next.config.ts`:

```typescript
async rewrites() {
  return {
    afterFiles: [
      { source: "/api/:path*", destination: "http://localhost:8001/api/:path*" },
      { source: "/uploads/:path*", destination: "http://localhost:8001/uploads/:path*" },
    ],
  };
}
```

Это позволяет фронтенду использовать относительные пути (`/api/listings`) без указания полного URL бэкенда.

**Как это работает:**
- **Development** (без nginx): Next.js проксирует `/api/*` → `http://localhost:8001/api/*`
- **Production** (с nginx): Nginx проксирует `/api/*` → `http://backend:8000/api/*`

Внутренние API-роуты Next.js (например `/api/site-icon`) обрабатываются самим Next.js благодаря использованию `afterFiles`.

---

## Инициализация базы данных (seed)

При первом развёртывании проекта необходимо инициализировать базу данных:

```bash
cd backend
.\\venv\\Scripts\\activate
python -m app.seed
```

### Что создаётся:

| Данные | Описание |
|--------|----------|
| **Справочники** | Виды использования (ИЖС, ЛПХ, СНТ, ДНП, СХ, Коммерческое), категории земель |
| **Админ** | `admin / Admin123` |
| **Настройки** | NSPD прокси/таймаут, DaData API ключ, **Telegram Bot Token** и **Chat ID** (конфигурируются в админке → Настройки) |

> ⚠️ Seed **не** создаёт районы, населённые пункты, риэлторов и объявления — они добавляются через админ-панель.

---

## Реализованные функции

### Страница объявления (`/listing/[slug]`)

- **Галерея изображений** — стрелки навигации (тёмный фон), миниатюры, счётчик, клавиатурная навигация
- **Карта участков** (React Leaflet + OpenStreetMap):
  - Полигоны границ участков
  - Цвета по статусу: 🟢 в продаже, 🟠 забронирован, 🔴 продан
  - Переключатель слоёв: карта / спутник
  - Popup с кадастровым номером и площадью
- **Диапазон площадей** — для объявлений с несколькими участками показывается "от X до Y сот."

### Страница карты (`/map`)

- **Интерактивная карта** всех объявлений с маркерами (Pin)
- **Маркеры всех участков** — для объявлений с несколькими участками отображаются все пины
- **Фильтры** — переиспользованы из каталога
- **Превью объявления** — при клике на маркер появляется карточка снизу
- **Авто-масштаб** — карта подстраивается под выборку (только при первой загрузке)

### Каталог (`/catalog`)

- **Фильтры по местоположению** — выбор населённых пунктов с группировкой по районам
- **Автоприменение фильтров** — при выборе населённых пунктов фильтры применяются сразу
- **Диапазон площадей** — для объявлений с несколькими участками показывается "X–Y сот."
- **Единая ширина контента** — согласованный дизайн на всех страницах (max-w-7xl)
 
 ### Обратная связь
 - **Модальное окно «Подберите мне участок»** — доступно из шапки сайта.
 - **Защита от спама (Honeypot)** — невидимые поля для блокировки ботов без капчи.
 - **Уведомления в Telegram** — мгновенная отправка данных заявки менеджеру.
 - **Админ-панель заявок** — просмотр всех обращений, управление статусами («Новый», «В работе», «Завершен»).

---

### SEO
- **Автоматическая sitemap.xml** и **robots.txt**
- **Динамические метаданные** (Title, Description, OpenGraph) для каждой страницы
- **Schema.org (JSON-LD)** разметка для организаций, товаров и статей
- **Динамический Favicon** — генерируется из SVG логотипа сайта (`/api/site-icon`)

---

## TODO

- [x] SEO: sitemap, robots.txt, metadata, schema.org
- [ ] Оптимизация производительности
- [ ] Загрузка изображений в CDN

